# Retired paths
/danmarks-mest-agentiske-mand  /agentisk-ingenioer  301

# Prerendered routes: serve the static snapshots explicitly (Netlify also
# resolves directory indexes on its own; these rules remove any doubt).
# The block between the markers is generated into dist/_redirects by
# scripts/prerender.mjs from src/routes.tsx; do not hand-edit it.
# routes:start
# routes:end

# SPA fallback for anything else
/*  /index.html  200
